import { type FC } from 'react';
import { Channels } from '../../components';

export const DashboardPage: FC = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <Channels />
    </div>
  );
};
